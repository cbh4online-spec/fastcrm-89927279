/**
 * App-mode configuration.
 *
 * O modo LeadChef foi removido. Apenas o modo `fastcrm` está disponível.
 * Este ficheiro mantém os símbolos por compatibilidade com imports legados.
 */

export type AppMode = "fastcrm";

/** Mantido vazio para compatibilidade. */
export const FASTCRM_PRODUCT_MODULES: string[] = [];
