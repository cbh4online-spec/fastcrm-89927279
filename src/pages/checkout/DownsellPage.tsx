import UpsellPage from "./UpsellPage";

// DownsellPage reuses UpsellPage with same logic — the offer content determines the visual
export default function DownsellPage() {
  return <UpsellPage />;
}
