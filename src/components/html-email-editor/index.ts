/**
 * HTML Email Editor — Phase 1 Core
 * 
 * Inline WYSIWYG editor for imported HTML email templates.
 * Renders HTML in an iframe, detects editable elements,
 * and provides a context panel for editing text, images, buttons, and links.
 */

export { HtmlEmailEditor } from './HtmlEmailEditor';
